"use strict"; // treat all JS code as newer js version

// alert("hello") // not able to use because we are using node insted of browser.

let name = "vishal"
let age = 24
let isLogedIn = false

/*
premitive datatypes:
    number => 2^53
    bigint
    string
    boolean
    null => stand alone value
    undefined
    symbol => unique

object
*/

// Note: type of null is object and type of undefined is undefined.

console.log(typeof undefined)